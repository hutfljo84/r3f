import type { Meta } from '@storybook/react';
import { Globus } from './Globus';

const Story: Meta<typeof Globus> = {
    component: Globus,
    title: 'Globus',
};
export default Story;

export const Primary = {
    args: {},
};
